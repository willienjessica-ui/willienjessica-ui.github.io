/* globals.css - Morris Lane: American Grit Edition */

:root {
  --navy: #1B263B;
  --crimson: #8E0E0E;
  --parchment: #F0EAD6;
  --brass: #B8860B;
  --iron: #333333;
  --leather: #3E2723;
  --gold-glow: rgba(184, 134, 11, 0.4);
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

html,
body {
  padding: 0;
  margin: 0;
  font-family: "Georgia", serif; /* Classic Americana feel */
  background: #1a1a1a;
  color: var(--parchment);
}

/* Layout */
.site-wrapper {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  background-color: #1a1a1a;
  background-image: 
    linear-gradient(to bottom, rgba(27, 38, 59, 0.9), rgba(26, 26, 26, 0.95)),
    url('https://www.transparenttextures.com/patterns/pinstriped-suit.png'); /* Distressed texture */
}

.site-main {
  flex: 1;
  max-width: 1400px;
  margin: 0 auto;
  padding: 1.5rem 1.25rem 3rem;
}

/* Header */
.site-header {
  border-bottom: 4px solid var(--brass);
  padding: 1rem 2rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  background: linear-gradient(90deg, var(--navy) 0%, #0d1b2a 100%);
  box-shadow: 0 4px 20px rgba(0,0,0,0.8);
}

.brand-block {
  display: flex;
  align-items: center;
  gap: 1.5rem;
}

.brand-crest {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  border: 3px solid var(--brass);
  object-fit: cover;
  box-shadow: 0 0 20px var(--gold-glow);
}

.brand-name {
  margin: 0;
  font-size: 2.2rem;
  font-weight: 900;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--parchment);
  text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
  font-family: "Impact", sans-serif;
}

.brand-tagline {
  margin: 0;
  font-size: 1rem;
  color: var(--brass);
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.2em;
}

/* Search Bar (Amazon Style but Grit) */
.search-container input {
  border: 2px solid var(--brass) !important;
  background: #fdfcf0 !important;
  color: #1a1a1a !important;
  font-weight: 600;
}

.search-container button {
  background: var(--brass) !important;
  color: white !important;
}

/* Hero Section */
.hero-section {
  position: relative;
  background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('/hero-bg.jpg');
  background-size: cover;
  background-position: center;
  padding: 6rem 3rem;
  border: 4px solid var(--brass);
  border-radius: 8px;
  margin-top: 2rem;
  text-align: center;
  box-shadow: inset 0 0 100px rgba(0,0,0,0.8);
}

.hero-title {
  font-size: 4.5rem;
  font-weight: 900;
  text-transform: uppercase;
  color: white;
  text-shadow: 4px 4px 0px var(--crimson), 8px 8px 0px var(--navy);
  margin-bottom: 1rem;
  font-family: "Impact", sans-serif;
}

.hero-subtitle {
  font-size: 1.5rem;
  color: var(--parchment);
  max-width: 800px;
  margin: 0 auto;
  font-weight: 600;
  text-shadow: 2px 2px 4px black;
}

/* Cards & Grid */
.product-card {
  background: #252525;
  border: 2px solid var(--iron);
  border-bottom: 5px solid var(--iron);
  padding: 1.5rem;
  transition: all 0.2s ease;
}

.product-card:hover {
  border-color: var(--brass);
  border-bottom-color: var(--crimson);
  transform: translateY(-3px);
}

.product-card h4 {
  color: var(--parchment);
  font-family: "Impact", sans-serif;
  text-transform: uppercase;
  font-size: 1.4rem;
}

.price-tag {
  background: var(--crimson);
  color: white;
  padding: 0.2rem 0.8rem;
  font-weight: 900;
  font-style: italic;
  display: inline-block;
  margin-bottom: 1rem;
}

/* USA BADGE */
.usa-badge {
  display: inline-block;
  padding: 0.4rem 1rem;
  background: var(--navy);
  color: white;
  font-weight: 900;
  font-size: 0.8rem;
  text-transform: uppercase;
  border: 2px solid var(--parchment);
  margin-bottom: 1rem;
}

/* AI DEBBIE */
.debbie-bubble {
  background: var(--navy) !important;
  color: white !important;
  border: 3px solid var(--brass) !important;
  font-family: "Impact", sans-serif;
  letter-spacing: 0.05em;
}

.debbie-avatar {
  border-color: var(--crimson) !important;
}

/* BUTTONS */
.btn-primary {
  background: var(--crimson) !important;
  border: 3px solid var(--parchment) !important;
  font-family: "Impact", sans-serif;
  font-size: 1.2rem !important;
}

.btn-secondary {
  background: var(--navy) !important;
  border: 3px solid var(--parchment) !important;
}

footer {
  background: #0a0a0a !important;
  border-top: 5px solid var(--crimson) !important;
}
